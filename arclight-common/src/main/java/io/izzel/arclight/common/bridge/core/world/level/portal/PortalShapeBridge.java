package io.izzel.arclight.common.bridge.core.world.level.portal;

import net.minecraft.world.level.LevelAccessor;

public interface PortalShapeBridge {

    boolean bridge$createPortal(LevelAccessor level);
}
